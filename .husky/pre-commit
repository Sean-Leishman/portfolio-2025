#!/bin/sh

# Sync public Obsidian notes to portfolio
npm run sync

# Stage any synced files
# -A so removed orphans are staged as deletions too
git add -A src/posts src/assets/blog 2>/dev/null || true
